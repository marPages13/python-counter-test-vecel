[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "my-nicegui-app"
version = "0.1.0"
description = "A simple NiceGUI web application"

[project.scripts]
start = "main:run"
